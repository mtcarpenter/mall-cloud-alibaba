server:
  port: 8083
spring:
  application:
    name: mall-admin-pms
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  main:
    #当遇到同样名字的时候，是否允许覆盖注册
    allow-bean-definition-overriding: true